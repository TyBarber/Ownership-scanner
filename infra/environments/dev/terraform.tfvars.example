aws_region  = "us-east-1"
environment = "dev"

enable_budget             = false
budget_limit_usd          = 5
budget_notification_email = ""

# Override only if the verified artifact is elsewhere.
# lambda_artifact_path = "../../../dist/ownership-scanner-lambda.zip"
